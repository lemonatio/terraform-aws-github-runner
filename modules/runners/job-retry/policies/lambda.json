{
  "Version": "2012-10-17",
  "Statement": [
%{ if length(github_app_ssm_parameter_arns) > 0 ~}
    {
      "Effect": "Allow",
      "Action": [
        "ssm:GetParameter",
        "ssm:GetParameters"
      ],
      "Resource": ${jsonencode(github_app_ssm_parameter_arns)}
    },
%{ endif ~}
%{ if length(github_app_secretsmanager_arns) > 0 ~}
    {
      "Effect": "Allow",
      "Action": [
        "secretsmanager:GetSecretValue"
      ],
      "Resource": ${jsonencode(github_app_secretsmanager_arns)}
    },
%{ endif ~}
    {
      "Effect": "Allow",
      "Action": [
        "sqs:ReceiveMessage",
        "sqs:GetQueueAttributes",
        "sqs:DeleteMessage"
      ],
      "Resource": "${sqs_job_retry_queue_arn}"
    },
    {
      "Effect": "Allow",
      "Action": [
        "sqs:SendMessage",
        "sqs:GetQueueAttributes"
      ],
      "Resource": "${sqs_build_queue_arn}"
%{ if kms_key_arn != "" ~}
    },
    {
        "Effect": "Allow",
        "Action": [
            "kms:Encrypt",
            "kms:Decrypt",
            "kms:GenerateDataKey"
        ],
        "Resource": "${kms_key_arn}"
%{ endif ~}
    }
  ]
}
